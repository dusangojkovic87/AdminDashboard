import { DropUploadImageDirective } from './drop-upload-image.directive';

describe('DropUploadImageDirective', () => {
  it('should create an instance', () => {
    const directive = new DropUploadImageDirective();
    expect(directive).toBeTruthy();
  });
});
