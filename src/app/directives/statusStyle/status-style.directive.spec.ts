import { StatusStyleDirective } from './status-style.directive';

describe('StatusStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusStyleDirective();
    expect(directive).toBeTruthy();
  });
});
