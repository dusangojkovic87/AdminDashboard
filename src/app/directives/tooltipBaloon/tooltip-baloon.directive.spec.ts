import { TooltipBaloonDirective } from './tooltip-baloon.directive';

describe('TooltipBaloonDirective', () => {
  it('should create an instance', () => {
    const directive = new TooltipBaloonDirective();
    expect(directive).toBeTruthy();
  });
});
